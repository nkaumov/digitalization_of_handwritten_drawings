# Experiments Directory

This directory stores reproducible experiment artifacts.

Expected structure:
- `runs/` — individual experiment runs;
- `leaderboards/` — accepted comparison tables;
- `templates/` — reusable summary templates.

Only accepted and reproducible experiment outputs should influence leaderboards.
