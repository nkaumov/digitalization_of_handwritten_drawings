# Research Artifacts

This folder is reserved for future research notebooks, experiments and temporary outputs.

Rules:
- Do not import production code from notebooks directly.
- Move stable logic into `app/` before using it in runtime paths.
- Keep large temporary artifacts under `research/artifacts/`.